import 'package:flutter/material.dart';

class AccuontPage extends StatelessWidget {
  const AccuontPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Accuonts'),
    );
  }
}