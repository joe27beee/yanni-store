import 'package:flutter/material.dart';

class AcousticGuitarPage extends StatelessWidget {
  const AcousticGuitarPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: null,
    );
  }
}